# Optional browser-visible configuration for local customization.
# AI keys are intentionally entered through the Settings UI and stored only in localStorage.

NEXT_PUBLIC_MARKET_API=https://api.coingecko.com/api/v3
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_AI_PROXY_URL=
